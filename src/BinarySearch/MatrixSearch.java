package BinarySearch;

public class MatrixSearch {
//    public int searchMatrix(int[][] matrix, int target) {
//        int rowCount = matrix.length;
//        int colCount = matrix[0].length;
//        if (target > matrix[colCount-1][rowCount-1] || target < matrix[0][0]) return -1;
//
//        int midRow = rowCount / 2;
//        int midCol = colCount / 2;
//        int minRow = 0;
//        int minCol = 0;
//        while (midRow < rowCount) {
//            if (matrix[minRow][minCol] > target) {
//                midRow = (minRow + midRow) / 2;
//            }
//            else if(matrix[minRow][minCol] < target) {
//                midRow = (midRow + rowCount) / 2;
//            }
//        }
//    }
}
